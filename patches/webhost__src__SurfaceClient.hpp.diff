# TV-relevant hunks only, extracted from a working tree by
# tools/extract-tv-patches.py. Apply by hand: this is not a patch
# series, and the surrounding file has other unreleased work in it.
diff --git a/webhost/src/SurfaceClient.hpp b/webhost/src/SurfaceClient.hpp
index e83feac1..e6e69f85 100644
--- a/webhost/src/SurfaceClient.hpp
+++ b/webhost/src/SurfaceClient.hpp
@@ -1,22 +1,25 @@
 #pragma once
 
 #include <filesystem>
 #include <array>
+#include <atomic>
 #include <chrono>
 #include <memory>
 #include <string>
 #include <unordered_map>
 #include <unordered_set>
 
 #include <d3d11_4.h>
 #include <wrl/client.h>
 
 #include <op77/WebUI/Messages.hpp>
 #include <op77/WebUI/SharedFrameBuffer.hpp>
 
+#include "AudioSink.hpp"
+
 #include "include/cef_client.h"
 #include "include/cef_request_handler.h"
 #include "include/cef_request_context.h"
 #include "include/cef_resource_request_handler.h"
 #include "include/wrapper/cef_message_router.h"
 
@@ -65,12 +69,29 @@ public:
 
     CefRefPtr<CefRenderHandler> GetRenderHandler() override { return this; }
     CefRefPtr<CefLifeSpanHandler> GetLifeSpanHandler() override { return this; }
     CefRefPtr<CefLoadHandler> GetLoadHandler() override { return this; }
     CefRefPtr<CefDisplayHandler> GetDisplayHandler() override { return this; }
     CefRefPtr<CefRequestHandler> GetRequestHandler() override { return this; }
+    // Every surface is a browser, so every surface can have audio: a page the
+    // resource ships may play a sound or a stream, and windowless CEF gives no
+    // device of its own. The PCM is handed to this process and played through
+    // AudioSink, which is why the surface feels like it has a speaker.
+    CefRefPtr<CefAudioHandler> GetAudioHandler() override { return this; }
+
+    // The device's shared-mode format has to be agreed before CEF starts a
+    // stream: a shared-mode client may not choose its own sample rate, so
+    // answering with CEF's 44.1 kHz default on a 48 kHz device would play every
+    // page fast and sharp. Probed once, before the first stream.
+    bool GetAudioParameters(CefRefPtr<CefBrowser> aBrowser, CefAudioParameters& aParams) override;
+    void OnAudioStreamStarted(CefRefPtr<CefBrowser> aBrowser, const CefAudioParameters& aParams,
+                              int aChannels) override;
+    void OnAudioStreamPacket(CefRefPtr<CefBrowser> aBrowser, const float** aData, int aFrames,
+                             int64_t aPts) override;
+    void OnAudioStreamStopped(CefRefPtr<CefBrowser> aBrowser) override;
+    void OnAudioStreamError(CefRefPtr<CefBrowser> aBrowser, const CefString& aMessage) override;
 
     bool OnProcessMessageReceived(CefRefPtr<CefBrowser> aBrowser, CefRefPtr<CefFrame> aFrame,
                                   CefProcessId aSourceProcess,
                                   CefRefPtr<CefProcessMessage> aMessage) override;
     void GetViewRect(CefRefPtr<CefBrowser> aBrowser, CefRect& aRect) override;
     bool GetScreenInfo(CefRefPtr<CefBrowser> aBrowser, CefScreenInfo& aScreenInfo) override;
@@ -80,12 +101,26 @@ public:
                  const RectList& aDirtyRects, const void* aBuffer, int aWidth, int aHeight) override;
     void OnAcceleratedPaint(CefRefPtr<CefBrowser> aBrowser, PaintElementType aType,
                             const RectList& aDirtyRects,
                             const CefAcceleratedPaintInfo& aInfo) override;
     void OnAfterCreated(CefRefPtr<CefBrowser> aBrowser) override;
     void OnBeforeClose(CefRefPtr<CefBrowser> aBrowser) override;
+    /// A framed page asking for a window.
+    ///
+    /// This is the whole of the popup problem: a site that pays for itself with
+    /// popups opens five of them on load, CEF would draw each as an overlay on
+    /// the same television -- `OnPopupShow`/`OnPopupSize` are exactly how this
+    /// host paints one -- and there is no title bar, no tab and no close button
+    /// to get out of them, so the page behind becomes unreachable. The policy is
+    /// in `op77/WebUI/AdBlock.hpp`; this is where it is applied.
+    bool OnBeforePopup(CefRefPtr<CefBrowser> aBrowser, CefRefPtr<CefFrame> aFrame, int aPopupId,
+                       const CefString& aTargetUrl, const CefString& aTargetFrameName,
+                       cef_window_open_disposition_t aDisposition, bool aUserGesture,
+                       const CefPopupFeatures& aFeatures, CefWindowInfo& aWindowInfo,
+                       CefRefPtr<CefClient>& aClient, CefBrowserSettings& aSettings,
+                       CefRefPtr<CefDictionaryValue>& aExtraInfo, bool* aNoJavaScriptAccess) override;
     void OnLoadEnd(CefRefPtr<CefBrowser> aBrowser, CefRefPtr<CefFrame> aFrame, int aHttpStatusCode) override;
     void OnLoadError(CefRefPtr<CefBrowser> aBrowser, CefRefPtr<CefFrame> aFrame,
                      ErrorCode aErrorCode, const CefString& aErrorText,
                      const CefString& aFailedUrl) override;
     bool OnConsoleMessage(CefRefPtr<CefBrowser> aBrowser, cef_log_severity_t aLevel,
                           const CefString& aMessage, const CefString& aSource,
@@ -102,12 +137,18 @@ public:
     void OnRenderProcessTerminated(CefRefPtr<CefBrowser> aBrowser,
                                    TerminationStatus aStatus, int aErrorCode,
                                    const CefString& aErrorString) override;
     CefRefPtr<CefResourceHandler> GetResourceHandler(CefRefPtr<CefBrowser> aBrowser,
                                                      CefRefPtr<CefFrame> aFrame,
                                                      CefRefPtr<CefRequest> aRequest) override;
+    /// Every request a framed page makes passes through here, which is what makes
+    /// a host-level blocklist possible at all: a script refused its window keeps
+    /// trying, and a script that never loads cannot.
+    cef_return_value_t OnBeforeResourceLoad(CefRefPtr<CefBrowser> aBrowser, CefRefPtr<CefFrame> aFrame,
+                                            CefRefPtr<CefRequest> aRequest,
+                                            CefRefPtr<CefCallback> aCallback) override;
     void OnProtocolExecution(CefRefPtr<CefBrowser> aBrowser, CefRefPtr<CefFrame> aFrame,
                              CefRefPtr<CefRequest> aRequest,
                              bool& aAllowOsExecution) override;
     bool OnQuery(CefRefPtr<CefBrowser> aBrowser, CefRefPtr<CefFrame> aFrame,
                  int64_t aQueryId, const CefString& aRequest, bool aPersistent,
                  CefRefPtr<Callback> aCallback) override;
@@ -155,12 +196,21 @@ private:
         uint64_t cpuFrames{};
         uint64_t popupFrames{};
     };
 
     [[nodiscard]] bool SameOrigin(const std::string& aUrl) const;
     [[nodiscard]] bool ResolveFile(const std::string& aUrl, std::filesystem::path& aPath) const;
+    /// The two same-origin routes a media page names (`/op77/media/probe`,
+    /// `/op77/media/stream`), served from this process through the staged
+    /// decoder. nullptr when the URL is not one of them, in which case the
+    /// ordinary file resolution runs.
+    [[nodiscard]] CefRefPtr<CefResourceHandler> MaybeServeTranscodeRoute(CefRefPtr<CefRequest> aRequest);
+    /// The third same-origin route a media page names (`/op77/web/frame?u=...`),
+    /// which answers whether a link can be framed and which application it wraps
+    /// when it cannot. Answered off the IO thread -- see `FrameResolveHandler`.
+    [[nodiscard]] CefRefPtr<CefResourceHandler> MaybeServeFrameRoute(CefRefPtr<CefRequest> aRequest);
     [[nodiscard]] static std::string MimeType(const std::filesystem::path& aPath);
     [[nodiscard]] static std::string JsonQuote(std::string_view aText);
 
     [[nodiscard]] bool EnsureGpuDevice(HANDLE aSourceHandle,
                                        Microsoft::WRL::ComPtr<ID3D11Texture2D>& aSource);
     [[nodiscard]] bool CreateDeviceOnAdapter(IDXGIAdapter1* aAdapter, std::string& aAdapterName);
@@ -228,16 +283,29 @@ private:
     bool m_sharedTextures{};
     bool m_gpuFallback{};
     bool m_ringFallbackAllowed{};
     std::string m_transport{"cpu"};
     bool m_externalBeginFrames{};
     std::chrono::steady_clock::time_point m_nextBeginFrame{};
+
+    std::unique_ptr<AudioSink> m_audio;
+    AudioSink::Format m_audioFormat{};
+    std::atomic<bool> m_audioFormatKnown{false};
+    std::atomic<bool> m_audioReported{false};
+    std::atomic<bool> m_audioFormatFailed{false};
     bool m_paintCallbackLogged{};
     bool m_acceleratedPaintCallbackLogged{};
     bool m_acceleratedPaintInfoLogged{};
     bool m_sourceOpenFailureLogged{};
     bool m_viewSizeMismatchLogged{};
 
+    // Window and request refusals, counted rather than merely traced: a page that
+    // pays for itself with advertising opens tens of windows a minute, and a
+    // traced line for each would bury the log that has to stay readable. The
+    // first few are named; the total is kept so the count is never lost.
+    std::atomic<uint64_t> m_blockedPopups{};
+    std::atomic<uint64_t> m_blockedRequests{};
+
     IMPLEMENT_REFCOUNTING(SurfaceClient);
     DISALLOW_COPY_AND_ASSIGN(SurfaceClient);
 };
 } // namespace op77::WebHost
